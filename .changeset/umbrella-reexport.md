---
"frontit": minor
---

Re-exports the reworked `toPx` from `@frontit/core`, which now takes an amount and a unit, measures inside any element, and can report every unit at once. See the [core changelog](../core/CHANGELOG.md) for what changed.
